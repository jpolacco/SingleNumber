/**
 * Finds the element that appears only once in an array where every
 * other element appears exactly twice.
 *
 * @param nums an array of integers in which every value appears twice
 *             except for one value that appears only once
 * @return the integer that appears only once in the array
 */
public int singleNumber(int[] nums) {
    // TO-DO: your code here
}
